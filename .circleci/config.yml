version: 2.0
jobs:
  build:
    working_directory: ~/repo
 
    steps:
      - checkout
      - run: mvn clean build
  test:
    working_directory: ~/repo
    steps:
        - checkout
        - run: mvn test
